# Baselines

Baseline repositories and downloaded baseline assets should live here.

Expected layout:

```text
baselines/CAFA/
baselines/ReWaS/
```

These directories are ignored by Git because they may contain third-party code, checkpoints, datasets, and generated outputs. Add setup scripts later if baseline installation needs to be reproduced automatically.
