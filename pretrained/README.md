# Pretrained Models

Place pretrained checkpoints here locally. Do not commit model files.

Recommended layout:

```text
pretrained/mmaudio/
pretrained/hunyuan-video-foley/
pretrained/etc/
```

Files such as `*.ckpt`, `*.pt`, `*.pth`, `*.safetensors`, and `*.bin` are ignored by Git.
