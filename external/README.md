# External Repositories

Run `bash scripts/setup_external_repos.sh` from the project root to clone the required external repositories into this directory.

Expected layout:

```text
external/MMAudio/
external/av-benchmark/
```

These cloned repositories are ignored by Git unless they are intentionally converted to submodules later.
